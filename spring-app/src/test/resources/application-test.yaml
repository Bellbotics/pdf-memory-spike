spring:
  main:
    banner-mode: "off"

management:
  datadog:
    metrics:
      export:
        enabled: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    io.netty.resolver.dns: WARN